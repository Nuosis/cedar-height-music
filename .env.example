# Phase 1 - Environment scaffold for Vite
# Copy to .env.development or .env.production as needed.
# Note: We do not modify real .env.* files without explicit permission.

# Base URL for live API (used in Data Layer when DATA_SOURCE=live)
VITE_API_BASE_URL=https://server.cedarheightsmusic.com/api/v1/

# Data source flag: mock | live
# mock = use local/mock data (Phase 4 will wire mocks)
# live = use public API endpoints
VITE_DATA_SOURCE=mock

# Brevo
VITE_BREVO_API_KEY=xkeysib-3c3907664c24af...
VITE_BREVO_FROM=example@email.com

# Site Maintenance
VITE_UNDER_MAINTENANCE=false